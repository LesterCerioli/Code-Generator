
class UserService {
  createUser(user) {
    // create user
  }

  sendEmail(user) {
    // send email
  }
}


class UserCreator {
  create(user) {
    // create user
  }
}

class EmailService {
  send(user) {
    // send email
  }
}
